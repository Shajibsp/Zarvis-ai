# codemagic.yaml
workflows:
  free-version:
    name: Free Version Build
    environment:
      groups: []
    scripts:
      - flutter build apk --release
    artifacts:
      - build/app/outputs/flutter-apk/*.apk
git add codemagic.yaml
git commit -m "Update Codemagic configuration"
git push origin main