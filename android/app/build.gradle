android {
    namespace "com.example.zaarvis_ai"   // ← নিজের প্যাকেজ নাম
    compileSdkVersion 34

    defaultConfig {
        applicationId "com.example.zaarvis_ai"
        minSdkVersion 21
        targetSdkVersion 34
        versionCode 1
        versionName "1.0"
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
}
