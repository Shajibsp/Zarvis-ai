# codemagic.yaml
workflows:
  free-version:
    name: Free Version Build
    environment:
      groups: [] # কোনো API কী লাগবে না
    scripts:
      - flutter build apk --release
    artifacts:
      - build/app/outputs/flutter-apk/*.apk
